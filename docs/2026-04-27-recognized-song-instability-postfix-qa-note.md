QA: `node validate-configs.cjs` passed, then `node server/run-pipeline.cjs --config configs/cod-test.yaml --verbose` refreshed `output/cod-test` and `benchmarks/fixtures/cod-test/_reports` (log: `.logs/cod-test-20260427-133555-ee-xjf5-rerun.log`). `music-vocals-data.json` preserved `recognizedSong.status: recognized`, `confidence: 0.95`, one `Master of Puppets` candidate with seven matched lyric fragments instead of collapsing to `unknown`. `famous-song-reconciliation.json` moved to `status: applied` / `trigger.passed: true` and removed six dialogue segments (`13,14,15,17,23,24`). Dialogue report improved to `dialogue_text_full_transcript_pct=92.9`, `dialogue_text_windowed_pct=93.1`, `dialogue_boundary_pct=0.0`, `output_segment_count=20`, `extra_output_window_count=0` versus the recent failed rerun `66.5 / 67.2 / 6` and the prior known-good post-fix run `81.3 / 81.7 / 1`. No provider/runtime instability surfaced during this rerun beyond the expected final benchmark failure (`0/7 artifacts passed`, `373/680` scoreable fields passed).